openapi: 3.1.0
info:
  title: openapi-fetch-mock
  version: "1.0"
paths:
  /cookie-parameter:
    parameters:
      - name: paramA
        in: cookie
        required: true
        schema:
          type: string
      - name: paramB
        in: cookie
        required: true
        schema:
          type: string
      - name: paramC
        in: cookie
        required: false
        schema:
          type: string
    get:
      responses:
        200:
          description: No Content
          content:
            application/json:
              schema:
                type: string
  /object-cookie-params:
    parameters:
      - name: object
        in: cookie
        required: true
        schema:
          type: object
          required: [foo, bar]
          properties:
            foo:
              type: string
            bar:
              type: number
    get:
      responses:
        200:
          description: No Content
          content:
            application/json:
              schema:
                type: string
  /no-cookie-params:
    get:
      responses:
        200:
          description: No Content
          content:
            application/json:
              schema:
                type: string
